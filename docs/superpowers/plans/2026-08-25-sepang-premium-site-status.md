# Premium Site Status

The redesign is being reviewed on `redesign-bg` before any merge to `main`.

Verified: branch separation, CNAME, robots, sitemap, bilingual entry points, supporting-page asset paths, and responsive interaction source contracts.